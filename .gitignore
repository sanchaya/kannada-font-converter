# Ignore system files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~

# Node
node_modules/
npm-debug.log

# Environment
.env
.env.local

# Dev-only / internal - kept locally, not published to the public repo.
# GitHub Pages itself only needs the static site (html/css/js/img) +
# test/stats.json, but server.js/package.json/package-lock.json ARE tracked
# (below) so the repo can be cloned straight onto a server and installed -
# see DEPLOYMENT.md.
.autopilot/
tools/
linkedin-post.md
test/permutations.js
test/ISSUES.md

# Node-server-only, opt-in submission log (see DEPLOYMENT.md) - never
# publish user-submitted conversion text.
data/